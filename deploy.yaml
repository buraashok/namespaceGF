---
- name: Generate template
  hosts: localhost
  tasks:
  - name: fail build
    fail:
      msg: "Label mismatch"
    when: value1!="valueone"
  - name: Generate pod template
    template:
      src: pod.yaml.j2
      dest: ./pod.yaml
