apiVersion: v1
kind: Pod
metadata:
  name: nginx
  namespace: {{ ns }}
  labels:
    name: nginx
    tag1: {{ projectkey }}
    AppID: {{ AppID }}
spec:
  containers:
  - name: nginx
    image: nginx
    ports:
    - containerPort: 80
